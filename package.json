{
  "name": "server-side-gestao-stock",
  "version": "1.0.0",
  "description": "creation of a back-end, gestao-stock, for an inventory management application that contains all the methods responsible for processing front-end data and making requests to the SQL databases, ensuring the system works",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "nodemon index.js"
  },
  "author": "Damaiceno Castro",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.2",
    "node": "^21.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
